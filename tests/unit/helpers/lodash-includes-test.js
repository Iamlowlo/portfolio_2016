
import { lodashIncludes } from 'portfolio-2016/helpers/lodash-includes';
import { module, test } from 'qunit';

module('Unit | Helper | lodash includes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lodashIncludes([42]);
  assert.ok(result);
});

